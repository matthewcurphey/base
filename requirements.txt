# Core data stack
pandas==2.2.2
SQLAlchemy==2.0.44
psycopg2-binary==2.9.11    # Postgres driver (analytics DB)
pyodbc==5.3.0              # SQL Server driver (Fabric lakehouse, config/lakehouse.py)

# Reports / Excel output
openpyxl==3.1.3
matplotlib==3.9.0

# Config / HTTP
python-dotenv==1.1.1
requests==2.32.3

# Browser automation for OBIEE report downloads (etl/extract/castle/obiee_download.py)
playwright==1.55.0

# Windows UI Automation for the Banner/Fabric Lakehouse sign-in popup
# (etl/utils/lakehouse_auth.py) — pyodbc's ActiveDirectoryInteractive auth
# spawns a native popup that Playwright can't attach to.
pywinauto==0.6.9
comtypes==1.4.16

# Windows-only: Outlook automation — sending (reports/*/*_email.py) and
# reading (etl/extract/oracle/oracle_download.py, via win32com) — this
# project only runs on Windows today.
pywin32==306

# dbt — invoked as a CLI (`dbt run`/`dbt seed`), not imported in Python,
# but required to build analytics_dbt/. profiles.yml is not checked into
# this repo (would contain credentials) — set up ~/.dbt/profiles.yml
# separately with profile name "analytics_dbt" (see analytics_dbt/dbt_project.yml).
dbt-core==1.10.15
dbt-postgres==1.9.1
